import { MovieClip } from "k8w-pixi-animate";
import { UIButton } from '../models/UIAdapter';

export class UI_IntroView extends MovieClip {
    tfHighScore: PIXI.Text;
    btnStart: UIButton;
    btnShop: UIButton;
    btnRank: UIButton;

    constructor();
}